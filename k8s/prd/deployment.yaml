apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-food-payments
  namespace: payments
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-food-payments
  template:
    metadata:
      labels:
        app: api-food-payments
    spec:
      containers:
        - name: api-food-payments
          image: postechf57/pos-tech-g57-app-payments:0.0.23
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: api-config-payments
            - secretRef:
                name: payments-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
